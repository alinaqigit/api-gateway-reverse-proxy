# Minimal Air config to build the actual main package.
root = "."

[build]
  cmd = "go build -o ./tmp/main ./cmd/api"
  bin = "./tmp/main"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", ".git"]

[log]
  time = true
